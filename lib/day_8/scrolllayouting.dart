import 'package:flutter/material.dart';

class scrolllayoutingDay7 extends StatelessWidget {
  const scrolllayoutingDay7({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        backgroundColor: Colors.amber,
        title: Text("belajar scroll dan layouting"),
        centerTitle: true,
      ),
    );
  }
}
